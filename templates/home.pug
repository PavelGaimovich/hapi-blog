extends layout

block content
  h1 Hello world!

  .row
    each article in articles
      .col.s12.m7
        .card.horizontal
          .card-stacked
            .card-content
              =article.body
            .card-action
              a(href='#') This is a link
